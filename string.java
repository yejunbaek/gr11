import java.util.Scanner;

public class string {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        // // 1
        // String userinput;
        // int middle;
        // System.out.print("Enter a number with more than 3 characters and has an odd
        // amount of characters: ");
        // userinput = input.next();
        // while (userinput.length() < 3 || userinput.length() % 2 == 0) {
        // if (userinput.length() < 3) {
        // System.out.println("Has less than 3 characters");
        // System.out.print("Enter a number with more than 3 characters and has an odd
        // amount of characters: ");
        // userinput = input.next();
        // } else {
        // System.out.println("Has an even number of characters");
        // System.out.print("Enter a number with more than 3 characters and has an odd
        // amount of characters: ");
        // userinput = input.next();

        // }

        // }
        // middle = userinput.length();
        // middle = middle / 2;
        // System.out.println(middle);

        // System.out.println("*" + userinput.substring(middle - 1, middle + 2) + "*");
        // 2
        // String userinput, password, password2;
        // System.out.print("Enter your username: ");
        // userinput = input.next();
        // System.out.print("Enter a password at least 8 characters long: ");
        // password = input.next();
        // while (password.length() < 8) {
        // System.out.print("Enter a password at least 8 characters long: ");
        // password = input.next();
        // }
        // password2 = password.toUpperCase();
        // password = password.toLowerCase();
        // System.out.println("Your username is " + userinput.toLowerCase() + " and your
        // password is "
        // + password2.substring(0, 4) + password.substring(4));
    }
}
